bleak>=0.22.3
cryptography>=44.0.0
anthropic>=0.45.0
python-dotenv>=1.0.1
winsdk>=1.0.0b10; sys_platform == "win32"  # classic-BT pairing experiment, see pair_glasses.py
pywin32>=306; sys_platform == "win32"  # COM apartment control (pythoncom) needed by rfcomm_pair.py
# voice assistant (myvu/voice.py): record the glasses' Windows HFP mic + Groq STT
sounddevice>=0.5.1
scipy>=1.14.0
numpy>=1.26.0
openai>=1.50.0  # used against Groq's OpenAI-compatible endpoint (needs GROQ_API_KEY)
# live navigation (myvu/gps.py): serial NMEA GPS receiver for `nav live`
pyserial>=3.5

