<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/obsidian_bg"
    android:fitsSystemWindows="true"
    android:paddingHorizontal="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="20dp">

    <!-- Kinetic Obsidian Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTrackpadBack"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/cyber_teal"
            app:cornerRadius="16dp"
            android:text="‹ Back" />

        <TextView
            style="@style/CardTitle.Myvu"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="20sp"
            android:text="Trackpad" />

        <!-- Spacer to keep the title centered against the Back button. -->
        <Space
            android:layout_width="72dp"
            android:layout_height="1dp" />
    </LinearLayout>

    <!-- Status indicator readout in Cyber Teal -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="8dp">

        <View
            android:id="@+id/trackpadStatusDot"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/status_dot" />

        <TextView
            android:id="@+id/txtTrackpadStatus"
            style="@style/Label.Myvu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="12sp"
            android:letterSpacing="0.05"
            android:textColor="@color/cyber_teal"
            android:gravity="center"
            android:text="Glasses not connected" />
    </LinearLayout>

    <!-- Main Touch Surface -->
    <com.myvu.client.ui.TrackpadView
        android:id="@+id/trackpad"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/trackpad_surface" />

    <!-- Touch Gesture Guidance (JetBrains Mono label-md) -->
    <TextView
        android:id="@+id/txtGestureHint"
        style="@style/Label.Myvu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textSize="12sp"
        android:letterSpacing="0.05"
        android:textColor="@color/on_surface_variant_obsidian"
        android:gravity="center"
        android:text="Tap = select · double-tap · long-press · swipe to move" />

</LinearLayout>
