<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="@color/obsidian_container"
    app:cardCornerRadius="28dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/outline_variant_obsidian"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Top Header Row: Category Badge & Date & Actions -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/lblNoteCategory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_chip_badge"
                android:fontFamily="monospace"
                android:letterSpacing="0.05"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:text="[TEXTO]"
                android:textColor="#E7D0FF"
                android:textSize="11sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/lblNoteDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="monospace"
                android:textColor="@color/on_surface_variant_obsidian"
                android:textSize="12sp" />

            <Space
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <ImageButton
                android:id="@+id/btnEditNote"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Editar nota"
                android:src="@android:drawable/ic_menu_edit"
                app:tint="@color/cyber_teal" />

            <ImageButton
                android:id="@+id/btnDeleteNote"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Eliminar nota"
                android:src="@android:drawable/ic_menu_delete"
                app:tint="#FF5252" />
        </LinearLayout>

        <!-- Body Section: Title & Content -->
        <TextView
            android:id="@+id/lblNoteTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/on_surface_obsidian"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <TextView
            android:id="@+id/lblNoteBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:lineSpacingExtra="2dp"
            android:textColor="@color/on_surface_obsidian"
            android:textSize="15sp" />

        <!-- Tag Chips Container -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroupTags"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            app:chipSpacingHorizontal="6dp"
            app:chipSpacingVertical="4dp" />

        <!-- Voice Note Extras: Waveform bars, duration counter, transcribe button, play button -->
        <LinearLayout
            android:id="@+id/layoutVoiceExtras"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Waveform Indicator Bars -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="24dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <View
                    android:layout_width="3dp"
                    android:layout_height="12dp"
                    android:layout_marginEnd="2dp"
                    android:background="@color/cyber_teal" />

                <View
                    android:layout_width="3dp"
                    android:layout_height="18dp"
                    android:layout_marginEnd="2dp"
                    android:background="@color/cyber_teal" />

                <View
                    android:layout_width="3dp"
                    android:layout_height="10dp"
                    android:layout_marginEnd="2dp"
                    android:background="@color/cyber_teal" />

                <View
                    android:layout_width="3dp"
                    android:layout_height="20dp"
                    android:layout_marginEnd="2dp"
                    android:background="@color/cyber_teal" />

                <View
                    android:layout_width="3dp"
                    android:layout_height="14dp"
                    android:background="@color/cyber_teal" />
            </LinearLayout>

            <!-- Duration Counter -->
            <TextView
                android:id="@+id/lblAudioDuration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="monospace"
                android:text="0:00"
                android:textColor="@color/on_surface_variant_obsidian"
                android:textSize="12sp" />

            <Space
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <!-- Transcribe STT Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnTranscribe"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:paddingHorizontal="8dp"
                android:text="Transcribir"
                android:textColor="@color/cyber_teal"
                android:textSize="12sp"
                app:icon="@android:drawable/ic_btn_speak_now"
                app:iconSize="16dp"
                app:iconTint="@color/cyber_teal" />

            <!-- Circular Audio Play/Pause Button -->
            <ImageButton
                android:id="@+id/btnPlayAudio"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Play Audio"
                android:src="@android:drawable/ic_media_play"
                android:visibility="gone"
                app:tint="@color/cyber_teal" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
